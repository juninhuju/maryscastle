// src/types/parse.d.ts

// Isso diz ao TypeScript que existe uma variável global chamada 'Parse'
// e que ela deve ser tratada como 'any' (tipo desconhecido), ignorando 
// as verificações estritas que causam o erro 2363.
declare var Parse: any;